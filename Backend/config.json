{
    "database": {
      "host": "${DB_HOST}",
      "port": "${DB_PORT}",
      "user": "${DB_USER}",
      "password": "${DB_PASSWORD}",
      "database": "${DB_NAME}"
    },
    "secret": "${JWT_SECRET}",
    "emailFrom": "${EMAIL_FROM}",
    "smtpOptions": {
      "host": "${SMTP_HOST}",
      "port": "${SMTP_PORT}",
      "auth": {
        "user": "${SMTP_USER}",
        "pass": "${SMTP_PASS}"
      }
    }
  }